# Copia este archivo a ".env.local" (ese sí está en .gitignore, nunca se sube) y
# coloca ahí tu API key real de Anthropic para que el botón "Redactar con IA"
# funcione. Sin esta variable, el reporte sigue funcionando con el texto
# generado por reglas (sin IA).
ANTHROPIC_API_KEY=
