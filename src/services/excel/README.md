# Excel

Responsabilidad futura: leer XLSX, validar columnas y convertir filas a `Ticket`.
La conversión de Time to Respond a minutos se implementará en otra fase.
No hay lectura, carga de archivos ni procesamiento implementados.
